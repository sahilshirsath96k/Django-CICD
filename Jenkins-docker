pipeline {
    agent {
        label 'ubuntu-agent'
    }

    stages {
        stage('Check Agent') {
            steps {
                sh 'whoami'
                sh 'docker --version'
                sh 'docker ps'
            }
        }
    }
}
